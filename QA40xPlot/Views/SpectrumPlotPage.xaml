<UserControl x:Class="QA40xPlot.Views.SpectrumPlotPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:views="clr-namespace:QA40xPlot.Views"
             xmlns:vm="clr-namespace:QA40xPlot.ViewModels"
             xmlns:local="clr-namespace:QA40xPlot" 
             d:DataContext="{d:DesignInstance Type=vm:SpectrumViewModel}"
                 d:Background="AliceBlue"
             mc:Ignorable="d" 
             d:DesignHeight="750" d:DesignWidth="800" >
    <Grid Height="Auto" Width="Auto">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
            <StackPanel Margin="0,1,1,0">
                <views:StartStop />
                <CheckBox Content="_Use Generator" IsChecked="{Binding UseGenerator1}" 
                      HorizontalAlignment="Center"  Margin="0,5,0,0" />
                <views:Attenuator />
                <Expander Header="—— Generator 1" IsExpanded="{Binding ExpandGenerator}" >
                    <Grid >
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"  MinWidth="{Binding LeftWidth}" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Label Content="Set _DUT" Target="{Binding ElementName=cbDUT}" 
                           HorizontalAlignment="Right" VerticalAlignment="Center"  Grid.Row="0" Grid.Column="0" ToolTip="{Binding DutInfo}"  />
                        <ComboBox ToolTip="{Binding DutDescript}" x:Name="cbDUT"  Grid.Column="1" Grid.ColumnSpan="2" Grid.Row="0" 
                            SelectedValue="{Binding GenDirection}" ItemsSource="{Binding MeasureVoltsFull}"
                              VerticalContentAlignment="Center"
                              IsEnabled="{Binding MainVm.IsNotRunning}" >
                        </ComboBox>
                        <Label Content="_Waveform" Target="{Binding ElementName=cbWave}"  
                           HorizontalAlignment="Right" VerticalAlignment="Center" Grid.Row="1" Grid.Column="0" ToolTip="{Binding DutInfo}"  />
                        <ComboBox x:Name="cbWave"  Grid.Column="1" Grid.ColumnSpan="2" Grid.Row="1"   
                              VerticalContentAlignment="Center"
                              SelectedValue="{Binding Gen1Waveform}" ItemsSource="{Binding Waveforms}">
                        </ComboBox>
                        <Label Content="Wav_e File" Target="{Binding ElementName=cbWName}"  
                            Visibility="{Binding Gen1Waveform, Converter={StaticResource EnumVisConverter}, ConverterParameter=WaveFile}"
                               HorizontalAlignment="Right" VerticalAlignment="Center" 
                               Grid.Row="2" Grid.Column="0" ToolTip="Select a WAV file."  />
                        <TextBox x:Name="cbWName"  Grid.Column="1" Grid.Row="2" MaxWidth="120"
                            Visibility="{Binding Gen1Waveform, Converter={StaticResource EnumVisConverter}, ConverterParameter=WaveFile}"
                              VerticalContentAlignment="Center"
                              Text="{Binding GenWavFile}" >
                        </TextBox>
                        <Button Visibility="{Binding Gen1Waveform, Converter={StaticResource EnumVisConverter}, ConverterParameter=WaveFile}"
                                Content="𝅘𝅥𝅮" MinWidth="20"
                                Grid.Row="2" Grid.Column="2" Command="{Binding DoWaveSelect}" />
                        <Label Content="Fre_quency" HorizontalAlignment="Right" VerticalAlignment="Center" 
                            Visibility="{Binding Gen1Waveform, Converter={StaticResource EnumNotVisConverter}, ConverterParameter=WaveFile}"
                               Grid.Row="3" Grid.Column="0" Target="{Binding ElementName=cbFreq}" />
                        <ComboBox x:Name="cbFreq" Grid.Column="1" Grid.Row="3"   
                            Visibility="{Binding Gen1Waveform, Converter={StaticResource EnumNotVisConverter}, ConverterParameter=WaveFile}"
                              VerticalContentAlignment="Center"
                               IsEditable="True" Text="{Binding Gen1Frequency}" ItemsSource="{Binding GenFrequencies}" MinWidth="80"/>
                        <Label Content="Hz" VerticalAlignment="Center" 
                            Visibility="{Binding Gen1Waveform, Converter={StaticResource EnumNotVisConverter}, ConverterParameter=WaveFile}"
                               Grid.Row="3" Grid.Column="2" />
                        <Label Content="{Binding GenAmpDescript, Mode=OneWay}"  Target="{Binding ElementName=cbVolt}" HorizontalAlignment="Right" VerticalAlignment="Center" Grid.Row="4" Grid.Column="0"  />
                        <ComboBox x:Name="cbVolt" Grid.Column="1" Grid.Row="4"   
                              VerticalContentAlignment="Center"
                              Text="{Binding Gen1Voltage}"
                              IsEditable="True" ItemsSource="{Binding GenVoltages}">
                        </ComboBox>
                        <views:VoltButton Grid.Row="4" Grid.Column="2" />
                    </Grid>
                </Expander>
                <views:SamplingExp />
                <views:SeeGlobals HorizontalAlignment="Stretch" Margin="0,5,0,0" />
                <views:StorageUi />
                <views:GotFileList HorizontalAlignment="Stretch" MaxHeight="150" Margin="0,5,0,0"/>
            </StackPanel>
        </ScrollViewer>
        <!--Main Plot Area-->
        <Grid Grid.Column="1" >
            <views:PlotControl x:Name="WpfPlot1"  VerticalAlignment="Stretch" HorizontalAlignment="Stretch" />
            <views:ThdChannelInfo x:Name="Info1" 
                  VerticalAlignment="Top" HorizontalAlignment="Left" Margin="60,70,0,0" />
            <views:ThdChannelInfo x:Name="Info2" 
                  VerticalAlignment="Top" HorizontalAlignment="Left" Margin="60,330,0,0" />
            <views:TabAbout x:Name="TAbout" 
                  VerticalAlignment="Top" HorizontalAlignment="Right" Margin="0,180,34,0" />
            <views:LegendWnd x:Name="LegendWindow" Visibility="{Binding ShowLegend, Converter={StaticResource BoolVisConverter}}"
                    VerticalAlignment="Top" HorizontalAlignment="Right" Margin="0,70,40,0" />
        </Grid>
        <!--End of main plot area-->
        <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
            <StackPanel Orientation="Vertical">
                <views:YAxisDb />
                <views:YAxisPerc />
                <views:XAxisHz />
                <Expander Header="—— Graph Data"  IsExpanded="{Binding ExpandGraphData}" >
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <CheckBox Content="Summary Data" ToolTip="{Binding SummaryDataTip}"
                              IsChecked="{Binding ShowSummary}" Grid.Row="0"  />
                        <CheckBox Content="Test Info" ToolTip="{Binding TabInfoTip}"
                              IsChecked="{Binding ShowTabInfo}" Grid.Row="1"  />
                        <CheckBox Content="Harmonic Markers" IsChecked="{Binding ShowMarkers}" Grid.Row="2"  />
                        <CheckBox Content="Power Markers" IsChecked="{Binding ShowPowerMarkers}" Grid.Row="3"  />
                    </Grid>
                </Expander>

                <Expander Header="—— Options"  IsExpanded="{Binding ExpandGraphOptions}" >
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <CheckBox Content="Left Channel" IsChecked="{Binding ShowLeft}" Grid.Row="0"  />
                        <CheckBox Content="Right Channel" IsChecked="{Binding ShowRight}" Grid.Row="1"  />
                        <CheckBox Content="Legend" ToolTip="{Binding ShowLegendTip}"
                            IsChecked="{Binding ShowLegend}" Grid.Row="2" Grid.Column="0"  />
                        <CheckBox Content="% - Data Summary" IsChecked="{Binding ShowDataPercent}" Grid.Row="3"  />
                        <CheckBox Content="Thick Lines" IsChecked="{Binding ShowThickLines}" Grid.Row="4"  />
                        <Button  Content="Edit Palette" Command="{Binding EditPaletteCommand}" Margin="12, 2" Grid.Row="5"></Button>
                    </Grid>
                </Expander>
                <views:CursorExpander />
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
