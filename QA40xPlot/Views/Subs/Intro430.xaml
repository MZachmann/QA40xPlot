<UserControl x:Class="QA40xPlot.Views.Intro430"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QA40xPlot.Views"
             xmlns:views="clr-namespace:QA40xPlot.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Expander Header="—— Variables" IsExpanded="{Binding ExpandVars}" 
              Visibility="{Binding HasQA430, Converter={StaticResource BoolVisConverter}}"  >
            <Grid >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="{Binding LeftWidth}" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" MinWidth="10" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <!-- do this strange StackPanel thing because dark mode messes up the checkbox alignments -->
                <StackPanel Grid.Row="0" Grid.Column="0" HorizontalAlignment="Right" VerticalAlignment="Center" >
                    <CheckBox Content="_Load" IsChecked="{Binding VaryLoad}" ToolTip="Default open if unchecked" Margin="0,0,10,0" />
                </StackPanel>
                <views:ComboDrop TheText="{Binding LoadSummary}" TheSource="{Binding QaLoads}" 
                                         IsEditable="False"
                                         Grid.Row="0" Grid.Column="1" 
                                         />
                <StackPanel Grid.Row="1" Grid.Column="0" HorizontalAlignment="Right" VerticalAlignment="Center" >
                    <CheckBox Content="_Gain" IsChecked="{Binding VaryGain}" ToolTip="Default 1 if unchecked"  Margin="0,0,10,0" />
                </StackPanel>
                <views:ComboDrop TheText="{Binding GainSummary}" TheSource="{Binding QaGains}" 
                                         IsEditable="False"
                                         Grid.Row="1" Grid.Column="1" 
                                         />
                <StackPanel Grid.Row="2" Grid.Column="0" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <CheckBox Content="_Supply" IsChecked="{Binding VarySupply}" Margin="0,0,10,0" 
                                      ToolTip="{Binding SupplyTip}" />
                </StackPanel>
                <views:ComboDrop TheText="{Binding SupplySummary}" TheSource="{Binding GenVoltages}" 
                                         IsEditable="True"
                                         Grid.Row="2" Grid.Column="1" 
                                         />
                <Label Content="V" Grid.Row="2" Grid.Column="2" />
            </Grid>
        </Expander>
</UserControl>
